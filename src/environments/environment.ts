export const environment = {
  // O CLI define este valor como 'true' no build de produção
  production: false, 
  
  // URL completa para desenvolvimento local (ng serve)
  apiBaseUrl: 'http://localhost:5000' 
};